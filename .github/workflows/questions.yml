name: Questions

on:
  push:
    branches: [main]
    paths:
      - 'questions/**'
      - 'src/shared/questions.ts'
      - '.github/workflows/questions.yml'
  schedule:
    - cron: '17 5 * * *'
  workflow_dispatch:

concurrency:
  group: questions
  cancel-in-progress: false

jobs:
  deploy:
    if: github.event_name != 'schedule'
    runs-on: ubuntu-24.04
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - uses: actions/checkout@v5
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: bun install --frozen-lockfile
      - run: bun run typecheck
      - run: bun test tests/questions.test.ts tests/questions-d1.test.ts tests/questions-server.test.ts

      - name: Apply the schema
        run: bunx wrangler d1 execute jobfill-questions --remote --yes --file=questions/schema.sql --config questions/wrangler.toml

      - name: Deploy the worker
        run: bunx wrangler deploy --config questions/wrangler.toml

  export:
    if: github.event_name != 'push'
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5

      - name: Take a copy of what is published
        run: |
          mkdir -p data
          curl -sfS --max-time 60 https://questions.jobfill.app/v1/questions | python3 -m json.tool > data/lexicon.json
          curl -sfS --max-time 60 https://questions.jobfill.app/v1/questions.csv > data/lexicon.csv

      - name: Commit it if it changed
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/lexicon.json data/lexicon.csv
          if git diff --cached --quiet; then
            echo "The published dataset has not changed."
            exit 0
          fi
          git commit -m "Update the published dataset"
          git push origin "HEAD:${GITHUB_REF_NAME}"
